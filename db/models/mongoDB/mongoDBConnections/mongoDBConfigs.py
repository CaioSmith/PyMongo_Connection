from configs.settings import (
    HOST, USER, PASSWORD, DB_NAME, PORT
)

# MongoDB configurations
mongo_db_infos = {
    "HOST": HOST,
    "PORT": PORT,
    "USER": USER,
    "PASSWORD": PASSWORD,
    "DB_NAME": DB_NAME
}
